<h3><%= @band.name %></h3>

<% @band.albums.each do |album| %>
  <ul>
    <li>
      <a href="<%= album_url(album.id) %>"><%= album.title %></a>
    </li>
  </ul>
<% end %>

<a href="<%= edit_band_url(@band.id) %>">Edit this band</a>

<br>

<a href="<%= new_band_album_url(@band.id) %>">Make a new album</a>

<a href="<%= bands_url %>">Index</a>