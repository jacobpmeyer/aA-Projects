import { Faker } from '../faker';
export declare class File {
    private readonly faker;
    constructor(faker: Faker);
    extension(): string;
    mimeType(): string;
    fileName(dir?: string | null, name?: string | null, ext?: string | null, directorySeparator?: string): string;
}
