import { Faker } from '../faker';
export declare class Number {
    private readonly faker;
    constructor(faker: Faker);
    number(digits?: number): number;
    decimal(leftDigits?: number, rightDigits?: number): string;
    normal(mean?: number, standardDeviation?: number): number;
    hexadecimal(digits?: number): string;
    between(from?: number, to?: number): number;
    betweenF(from?: number, to?: number): number;
    positive(from?: number, to?: number): number;
    negative(from?: number, to?: number): number;
    digit(): number;
    private nonZeroDigit;
    private decimalPart;
    private leadingZeroNumber;
}
