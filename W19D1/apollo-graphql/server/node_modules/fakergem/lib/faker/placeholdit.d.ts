import { Faker } from '../faker';
export declare class Placeholdit {
    private SUPPORTED_FORMATS;
    private readonly faker;
    constructor(faker: Faker);
    image(size?: string, format?: string, backgroundColor?: string | null, textColor?: string | null, text?: string | null): string;
    private isValidSize;
    private isSupportedFormat;
    private isValidColorValue;
}
