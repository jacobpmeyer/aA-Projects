import { Faker } from '../faker';
export declare class Superhero {
    private readonly faker;
    constructor(faker: Faker);
    name(): string;
    power(): string;
    prefix(): string;
    suffix(): string;
    descriptor(): string;
}
