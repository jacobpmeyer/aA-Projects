import { Faker } from '../faker';
export declare class PhoneNumber {
    private readonly faker;
    constructor(faker: Faker);
    phoneNumber(): string;
    cellPhone(): string;
    areaCode(): string;
    exchangeCode(): string;
    subscriberNumber(length?: number): string;
    extension(length?: number): string;
}
