import { Faker } from '../faker';
export declare class Lorem {
    private readonly faker;
    constructor(faker: Faker);
    word(): string;
    words(num?: number, supplemental?: boolean): string[];
    character(): string;
    characters(charCount?: number): string;
    sentence(wordCount?: number, supplemental?: boolean, randomWordsToAdd?: number): string;
    sentences(sentenceCount?: number, supplemental?: boolean): string[];
    paragraph(sentenceCount?: number, supplemental?: boolean, randomSentencesToAdd?: number): string;
    paragraphs(paragraphCount?: number, supplemental?: boolean): string[];
}
