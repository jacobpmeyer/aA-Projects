"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var file_json_1 = __importDefault(require("../data/file.json"));
var File = /** @class */ (function () {
    function File(faker) {
        this.faker = faker;
    }
    File.prototype.extension = function () {
        return this.faker.Random.element(file_json_1.default.extensions);
    };
    File.prototype.mimeType = function () {
        return this.faker.Random.element(file_json_1.default.mimeTypes);
    };
    File.prototype.fileName = function (dir, name, ext, directorySeparator) {
        if (dir === void 0) { dir = null; }
        if (name === void 0) { name = null; }
        if (ext === void 0) { ext = null; }
        if (directorySeparator === void 0) { directorySeparator = '/'; }
        dir = dir || this.faker.Lorem.words(2).join('-');
        name = name || this.faker.Lorem.word();
        ext = ext || this.extension();
        return [dir, name].join(directorySeparator) + "." + ext;
    };
    return File;
}());
exports.File = File;
