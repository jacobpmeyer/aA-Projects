import { Faker } from '../faker';
export declare class Bank {
    private readonly faker;
    constructor(faker: Faker);
    name(): string;
    swiftBic(): string;
    iban(bankCountryCode?: string): string;
}
