import { Faker } from '../faker';
export declare class HarryPotter {
    private readonly faker;
    constructor(faker: Faker);
    character(): string;
    location(): string;
    quote(): string;
    book(): string;
}
