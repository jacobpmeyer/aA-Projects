import { Faker } from '../faker';
export declare class LoremFlickr {
    private SUPPORTED_COLORIZATIONS;
    private readonly faker;
    constructor(faker: Faker);
    image(size?: string, searchTerms?: string[], matchAll?: boolean): string;
    grayscaleImage(size?: string, searchTerms?: string[], matchAll?: boolean): string;
    pixelatedImage(size?: string, searchTerms?: string[], matchAll?: boolean): string;
    colorizedImage(size?: string, color?: string, searchTerms?: string[], matchAll?: boolean): string;
    private buildUrl;
    private hasSearchTerms;
    private isColorizationSupported;
    private isValidSize;
}
