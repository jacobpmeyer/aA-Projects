"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var bank_json_1 = __importDefault(require("../data/bank.json"));
var Bank = /** @class */ (function () {
    function Bank(faker) {
        this.faker = faker;
    }
    Bank.prototype.name = function () {
        return this.faker.Random.element(bank_json_1.default.names);
    };
    Bank.prototype.swiftBic = function () {
        return this.faker.Random.element(bank_json_1.default.swiftBics);
    };
    Bank.prototype.iban = function (bankCountryCode) {
        var _this = this;
        if (bankCountryCode === void 0) { bankCountryCode = 'GB'; }
        var details = bank_json_1.default.ibanDetails.filter(function (x) { return x.bankCountryCode === bankCountryCode.toUpperCase(); })[0];
        var bcc = "" + details.bankCountryCode + this.faker.Number.between(10, 99);
        var ilc = Array(parseInt(details.ibanLetterCode, 10))
            .fill('0')
            .map(function () { return String.fromCharCode(65 + _this.faker.Number.between(0, 25)); })
            .join('');
        var ib = Array(parseInt(details.ibanDigits, 10))
            .fill('')
            .map(function () { return _this.faker.Number.between(0, 9); })
            .join('');
        return "" + bcc + ilc + ib;
    };
    return Bank;
}());
exports.Bank = Bank;
