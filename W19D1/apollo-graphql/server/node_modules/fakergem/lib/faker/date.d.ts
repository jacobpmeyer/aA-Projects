import { Faker } from '../faker';
export declare class DateFaker {
    private readonly faker;
    constructor(faker: Faker);
    between(from: Date | string, to: Date | string): Date;
    betweenExcept(from: Date | string, to: Date | string, except: Date | string): Date;
    forward(days?: number): Date;
    backward(days?: number): Date;
    birthday(minAge?: number, maxAge?: number): Date;
    private getDateObject;
    private datesAreEqual;
    private daysFromNow;
}
