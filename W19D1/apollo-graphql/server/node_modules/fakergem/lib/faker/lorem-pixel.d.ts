import { Faker } from '../faker';
export declare class LoremPixel {
    private SUPPORTED_CATEGORIES;
    private readonly faker;
    constructor(faker: Faker);
    image(size?: string, isGray?: boolean, category?: string | null, num?: number | null, text?: string | null): string;
    private isValidSize;
    private isCategorySupported;
    private isNumberValid;
}
