import { Faker } from '../faker';
export declare class Commerce {
    private readonly faker;
    constructor(faker: Faker);
    color(): string;
    department(max?: number, fixedAmount?: boolean): string;
    productName(): string;
    price(range?: {
        min: number;
        max: number;
    }): string;
    promotionCode(digits?: number): string;
    private buildCategories;
    private mergeCategories;
}
