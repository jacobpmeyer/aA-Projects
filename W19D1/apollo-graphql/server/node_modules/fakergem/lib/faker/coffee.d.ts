import { Faker } from '../faker';
export declare class Coffee {
    private readonly faker;
    constructor(faker: Faker);
    blendName(): string;
    origin(): string;
    variety(): string;
    notes(): string;
    private parse;
}
