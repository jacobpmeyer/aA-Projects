"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Fillmurray = /** @class */ (function () {
    function Fillmurray(faker) {
        this.faker = faker;
    }
    Fillmurray.prototype.image = function (grayscale, width, height) {
        if (grayscale === void 0) { grayscale = false; }
        if (width === void 0) { width = 200; }
        if (height === void 0) { height = 200; }
        if (!this.isValidNumber(width)) {
            throw new Error('Width should be a number');
        }
        if (!this.isValidNumber(height)) {
            throw new Error('Height should be a number');
        }
        if (!this.isValidBoolean(grayscale)) {
            throw new Error('Grayscale should be a boolean');
        }
        return ['https://fillmurray.com', grayscale ? 'g' : null, width, height].filter(function (x) { return x; }).join('/');
    };
    Fillmurray.prototype.isValidNumber = function (value) {
        return /^\d+$/.test(value.toString());
    };
    Fillmurray.prototype.isValidBoolean = function (value) {
        return /(true|false)/.test(value.toString());
    };
    return Fillmurray;
}());
exports.Fillmurray = Fillmurray;
