"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var collection_helpers_1 = require("../utils/collection-helpers");
var Random = /** @class */ (function () {
    function Random(faker) {
        this.faker = faker;
    }
    Random.prototype.assortment = function (array, n) {
        var count = Math.max(0, n);
        var repeatCount = Math.floor(count / array.length) || 1;
        var repeated = Array(repeatCount * array.length)
            .fill(null)
            .map(function (e, i) { return array[i % array.length]; });
        return collection_helpers_1.shuffle(repeated).slice(0, count);
    };
    Random.prototype.element = function (array) {
        return collection_helpers_1.pickOne(array);
    };
    return Random;
}());
exports.Random = Random;
