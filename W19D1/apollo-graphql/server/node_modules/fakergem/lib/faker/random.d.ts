import { Faker } from '../faker';
export declare class Random {
    private readonly faker;
    constructor(faker: Faker);
    assortment(array: any[], n: number): any[];
    element(array: any[] | string): any;
}
